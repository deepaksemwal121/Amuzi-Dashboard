import axios from "axios";
import React from "react";

const ShowVideosInPlaylist = () => {
  const getPlaylistvideos = async () => {
    try {
      // const res = await axios.get();
    } catch (err) {}
  };

  return <div className="overflow-x-scroll video">ShowVideosInPlaylist</div>;
};

export default ShowVideosInPlaylist;
